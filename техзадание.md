# Техническое задание: Портирование Solana Wallet Monitor на Rust

## 🎯 Цель проекта

Создать высокопроизводительный бот на Rust для мониторинга кошелька Solana и автоматического копирования сделок (copy-trading) с минимальной латентностью (<100ms от обнаружения до исполнения).

## 📊 Требования к производительности

### Критичные метрики:
- **WebSocket → обработка**: < 5ms
- **gRPC → обработка**: < 2ms (быстрее за счет бинарного протокола)
- **Парсинг транзакции**: < 3ms
- **Race запрос (3 RPC)**: < 50ms
- **Jupiter quote**: < 100ms
- **Создание и подпись транзакции**: < 10ms
- **Отправка транзакции**: < 50ms
- **Общая латентность (WebSocket)**: < 200ms
- **Общая латентность (gRPC)**: < 150ms (на 25% быстрее)

### Целевые улучшения:
- **4-5x быстрее** чем Python версия
- **3-5x меньше** потребление памяти
- **99.9% uptime** с автоматическим переподключением

---

## 🏗️ Архитектура системы

### Общая схема потоков данных

```
┌─────────────────────────────────────────────────────────────┐
│              Transport Layer (WebSocket/gRPC)              │
│  ┌────────────────────┐      ┌────────────────────┐     │
│  │ WebSocket Manager  │      │   gRPC Manager      │     │
│  │ (tokio-tungstenite)│      │   (tonic, streaming)│     │
│  │ - Fan-out support  │      │ - Binary protocol   │     │
│  │ - Auto reconnect   │      │ - Lower latency     │     │
│  └─────────┬──────────┘      └──────────┬──────────┘     │
│            │                            │                │
│            └────────────┬───────────────┘                │
│                         │ (configurable via env)          │
└─────────────────────────┼────────────────────────────────┘
                           │ signatures (unbounded channel)
                           ▼
┌─────────────────────────────────────────────────────────────┐
│              Transaction Signature Queue                    │
│         (mpsc::UnboundedReceiver<String>)                    │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ├──► Worker 1 ──► Race RPC ──► Parse ──► Cache
                       ├──► Worker 2 ──► Race RPC ──► Parse ──► Cache
                       └──► Worker N ──► Race RPC ──► Parse ──► Cache
                                              │
                                              ▼
                                    ┌─────────────────┐
                                    │  Swap Detected  │
                                    └────────┬────────┘
                                             │
                                             ▼
┌─────────────────────────────────────────────────────────────┐
│                  Trading Engine                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  Buy Handler │  │  Sell Handler│  │ Risk Check   │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                 │                  │              │
│         └─────────────────┴──────────────────┘              │
│                            │                                │
│                            ▼                                │
│              ┌──────────────────────────┐                   │
│              │  Jupiter Client (async)  │                   │
│              │  - Quote (race)          │                   │
│              │  - Create Swap TX        │                   │
│              └─────────────┬────────────┘                   │
│                            │                                │
│                            ▼                                │
│              ┌──────────────────────────┐                   │
│              │  Sign Transaction        │                   │
│              └─────────────┬────────────┘                   │
│                            │                                │
│                            ▼                                │
│              ┌──────────────────────────┐                   │
│              │  Shotgun Send (race)    │                   │
│              │  (all RPC endpoints)    │                   │
│              └──────────────────────────┘                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 Структура проекта Rust

```
solana-wallet-monitor/
├── Cargo.toml
├── .env.example
├── README.md
├── src/
│   ├── main.rs                 # Точка входа, инициализация
│   ├── config.rs               # Конфигурация (dotenv)
│   ├── error.rs                # Типы ошибок (thiserror)
│   │
│   ├── transport/              # Transport модуль (WebSocket/gRPC)
│   │   ├── mod.rs              # Общий интерфейс и переключение
│   │   ├── trait.rs            # Transport trait для абстракции
│   │   │
│   │   ├── websocket/          # WebSocket реализация
│   │   │   ├── mod.rs
│   │   │   ├── manager.rs      # WebSocket менеджер с reconnect
│   │   │   └── handler.rs      # Обработка сообщений
│   │   │
│   │   └── grpc/               # gRPC реализация
│   │       ├── mod.rs
│   │       ├── client.rs        # gRPC клиент (tonic)
│   │       ├── streaming.rs     # Streaming подписки
│   │       └── proto/           # Protobuf определения
│   │           └── solana.proto # Solana gRPC proto файлы
│   │
│   ├── http/                   # HTTP клиент
│   │   ├── mod.rs
│   │   ├── race_client.rs      # Race запросы к RPC
│   │   ├── rate_limiter.rs     # Rate limiting
│   │   └── pool.rs             # Connection pooling
│   │
│   ├── processor/              # Обработка транзакций
│   │   ├── mod.rs
│   │   ├── transaction.rs      # Парсинг транзакций
│   │   ├── swap_detector.rs    # Обнаружение swap
│   │   ├── cache.rs            # Lock-free кэш (DashMap)
│   │   └── worker.rs           # Worker pool
│   │
│   ├── trading/                # Торговый модуль
│   │   ├── mod.rs
│   │   ├── engine.rs           # Торговый движок
│   │   ├── jupiter.rs          # Jupiter API клиент
│   │   ├── signer.rs           # Подпись транзакций
│   │   └── risk.rs             # Risk management
│   │
│   ├── analytics/              # Аналитика
│   │   ├── mod.rs
│   │   └── stats.rs            # Статистика и метрики
│   │
│   └── utils/                  # Утилиты
│       ├── mod.rs
│       ├── token.rs            # Token metadata
│       └── time.rs             # Временные утилиты
│
├── tests/                      # Интеграционные тесты
│   ├── websocket_test.rs
│   ├── processor_test.rs
│   └── trading_test.rs
│
└── benches/                    # Бенчмарки
    ├── http_bench.rs
    ├── parse_bench.rs
    └── swap_bench.rs
```

---

## 🔧 Технологический стек

### Основные зависимости:

```toml
[dependencies]
# Async runtime
tokio = { version = "1.35", features = ["full", "rt-multi-thread", "time"] }

# Transport (WebSocket/gRPC)
tokio-tungstenite = { version = "0.21", features = ["native-tls"] }
futures-util = "0.3"

# gRPC
tonic = "0.11"                    # gRPC framework
tonic-build = "0.11"              # Build tool для proto
prost = "0.12"                    # Protobuf runtime
prost-types = "0.12"

# HTTP клиент
reqwest = { version = "0.11", features = ["json", "rustls-tls", "http2", "stream"] }

# Solana SDK
solana-sdk = "1.18"
solana-client = "1.18"
solana-transaction-status = "1.18"
solana-account-decoder = "1.18"

# Криптография
ed25519-dalek = "2.1"
bs58 = "0.5"
base64 = "0.21"

# Сериализация
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0", features = ["raw_value"] }

# Структуры данных
dashmap = "5.5"              # Lock-free HashMap
parking_lot = "0.12"         # Быстрые мьютексы
arc-swap = "1.6"             # Lock-free Arc swap

# Конфигурация
config = "0.14"
dotenv = "0.15"

# Ошибки
anyhow = "1.0"
thiserror = "1.0"

# Логирование
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Утилиты
chrono = "0.4"
uuid = { version = "1.6", features = ["v4"] }
```

### Оптимизации сборки:

```toml
[profile.release]
opt-level = 3              # Максимальная оптимизация
lto = true                 # Link-time optimization
codegen-units = 1          # Лучшая оптимизация
panic = "abort"            # Меньше размер
strip = true              # Удалить символы
```

---

## 🚀 Последовательность выполнения задач

### Фаза 1: Инфраструктура (Неделя 1)

#### Задача 1.1: Конфигурация и ошибки
**Приоритет**: Высокий  
**Время**: 4 часа  
**Зависимости**: Нет

- [ ] Создать `src/config.rs` с загрузкой из `.env`
- [ ] Создать `src/error.rs` с типами ошибок
- [ ] Настроить `Cargo.toml` с базовыми зависимостями

**Критерии готовности**:
- Конфигурация загружается из `.env`
- Все параметры из Python config.py перенесены
- Типы ошибок покрывают все случаи

---

#### Задача 1.2: HTTP Race Client
**Приоритет**: Критичный  
**Время**: 8 часов  
**Зависимости**: 1.1

- [ ] Создать `src/http/race_client.rs`
- [ ] Реализовать connection pooling с `reqwest`
- [ ] Реализовать race запросы (`get_tx_race`, `send_tx_race`)
- [ ] Добавить rate limiting
- [ ] Добавить retry с exponential backoff

**Критерии готовности**:
- Race запросы работают параллельно на все RPC
- Первый успешный ответ возвращается немедленно
- Connection pooling переиспользует соединения
- Rate limiting предотвращает блокировки

**Оптимизации**:
- Использовать HTTP/2 для мультиплексирования
- Минимальные таймауты (300-500ms)
- Zero-copy где возможно

---

#### Задача 1.3: Transport Layer (WebSocket/gRPC)
**Приоритет**: Критичный  
**Время**: 12 часов  
**Зависимости**: 1.1

##### Подзадача 1.3.1: Transport Trait
- [ ] Создать `src/transport/trait.rs` с общим интерфейсом
- [ ] Определить `Transport` trait с методами:
  - `connect()` - подключение
  - `subscribe_logs()` - подписка на логи
  - `receive_signatures()` - получение сигнатур
  - `reconnect()` - переподключение

##### Подзадача 1.3.2: WebSocket Manager
- [ ] Создать `src/transport/websocket/manager.rs`
- [ ] Реализовать подключение с `tokio-tungstenite`
- [ ] Реализовать автоматическое переподключение
- [ ] Реализовать fan-out (несколько WS одновременно)
- [ ] Создать канал для отправки сигнатур
- [ ] Реализовать `Transport` trait для WebSocket

**Критерии готовности WebSocket**:
- WebSocket подключается и подписывается на логи
- Автоматическое переподключение при обрыве
- Сигнатуры транзакций отправляются в канал без блокировок
- Поддержка нескольких WS endpoints (fan-out)

**Оптимизации WebSocket**:
- Zero-copy парсинг JSON
- Неблокирующая отправка в канал
- Минимальная обработка в WS handler

##### Подзадача 1.3.3: gRPC Client
- [ ] Создать `src/transport/grpc/client.rs`
- [ ] Настроить `tonic` для gRPC клиента
- [ ] Создать proto файлы для Solana gRPC (если доступны)
- [ ] Реализовать streaming подписки на транзакции
- [ ] Реализовать автоматическое переподключение
- [ ] Реализовать `Transport` trait для gRPC

**Критерии готовности gRPC**:
- gRPC подключается к Solana gRPC endpoint
- Streaming подписки работают корректно
- Автоматическое переподключение при обрыве
- Сигнатуры транзакций отправляются в канал без блокировок
- Латентность < 2ms (быстрее WebSocket)

**Оптимизации gRPC**:
- Бинарный протокол (быстрее JSON)
- Streaming для минимальной задержки
- Connection pooling
- Keepalive для стабильности

##### Подзадача 1.3.4: Transport Selector
- [ ] Создать `src/transport/mod.rs` с фабрикой
- [ ] Реализовать переключение между WebSocket и gRPC через конфигурацию
- [ ] Добавить параметр `TRANSPORT_MODE` в config (ws/grpc/auto)
- [ ] Реализовать автоматический выбор (auto mode) на основе доступности

**Критерии готовности**:
- Переключение между WebSocket и gRPC работает через конфигурацию
- Auto mode выбирает лучший доступный транспорт
- Единый интерфейс для обоих транспортов
- Graceful fallback при недоступности gRPC

**Конфигурация**:
```rust
// config.rs
pub enum TransportMode {
    WebSocket,  // Использовать только WebSocket
    Grpc,       // Использовать только gRPC
    Auto,       // Автоматический выбор (gRPC предпочтительнее)
}

// .env
TRANSPORT_MODE=auto  # или "ws" или "grpc"
GRPC_ENDPOINT=https://grpc.solana.com  # gRPC endpoint

// Примеры gRPC endpoints для разных провайдеров:
// Helius: https://mainnet.helius-rpc.com/?api-key=YOUR_KEY (если поддерживают gRPC)
// QuickNode: https://YOUR_ENDPOINT.solana-mainnet.quiknode.pro/YOUR_KEY (если поддерживают gRPC)
// Syndica: https://solana-api.syndica.io/access-token/YOUR_TOKEN (если поддерживают gRPC)
// Примечание: Не все провайдеры поддерживают gRPC, проверьте документацию провайдера
```

**Примеры использования**:

```rust
// Автоматический выбор (рекомендуется)
TRANSPORT_MODE=auto
GRPC_ENDPOINT=https://grpc.solana.com
WEBSOCKET_URL=wss://api.mainnet-beta.solana.com

// Принудительное использование gRPC
TRANSPORT_MODE=grpc
GRPC_ENDPOINT=https://grpc.solana.com

// Принудительное использование WebSocket
TRANSPORT_MODE=ws
WEBSOCKET_URL=wss://api.mainnet-beta.solana.com
```

**Провайдеры с поддержкой gRPC** (требуется проверка актуальности):

1. **Helius** (если поддерживают):
   - gRPC endpoint: проверьте документацию Helius
   - Преимущества: низкая латентность, высокая пропускная способность

2. **QuickNode** (если поддерживают):
   - gRPC endpoint: проверьте документацию QuickNode
   - Преимущества: надежность, хорошая производительность

3. **Syndica** (если поддерживают):
   - gRPC endpoint: проверьте документацию Syndica
   - Преимущества: специализация на Solana

4. **Собственный gRPC сервер**:
   - Можно развернуть собственный gRPC proxy для максимальной производительности
   - Использовать Solana gRPC proto определения

**Примечание**: Не все провайдеры поддерживают gRPC. Рекомендуется:
- Использовать `TRANSPORT_MODE=auto` для автоматического выбора
- Проверить документацию провайдера перед использованием gRPC
- Иметь fallback на WebSocket при недоступности gRPC

---

### Фаза 2: Обработка транзакций (Неделя 1-2)

#### Задача 2.1: Парсинг транзакций
**Приоритет**: Критичный  
**Время**: 8 часов  
**Зависимости**: 1.2

- [ ] Создать `src/processor/transaction.rs`
- [ ] Реализовать парсинг JSON транзакции
- [ ] Извлечение token balances (pre/post)
- [ ] Извлечение SOL баланса
- [ ] Вычисление изменений (increases/decreases)

**Критерии готовности**:
- Парсинг транзакции < 3ms
- Корректное извлечение всех балансов
- Поддержка jsonParsed encoding

**Оптимизации**:
- Использовать `serde_json::raw_value` для больших полей
- Минимальные аллокации
- Inline функции для горячих путей

---

#### Задача 2.2: Swap Detector
**Приоритет**: Критичный  
**Время**: 4 часа  
**Зависимости**: 2.1

- [ ] Создать `src/processor/swap_detector.rs`
- [ ] Реализовать обнаружение SOL → TOKEN swap
- [ ] Реализовать обнаружение TOKEN → SOL swap
- [ ] Вычисление цены и количества

**Критерии готовности**:
- Обнаружение swap < 1ms
- Корректное определение типа swap
- Извлечение всех необходимых данных

---

#### Задача 2.3: Lock-free Cache
**Приоритет**: Высокий  
**Время**: 3 часа  
**Зависимости**: Нет

- [ ] Создать `src/processor/cache.rs`
- [ ] Использовать `DashMap` для lock-free кэша
- [ ] Реализовать LRU eviction
- [ ] Настроить максимальный размер

**Критерии готовности**:
- Кэш работает без блокировок
- LRU eviction работает корректно
- Производительность O(1) для get/set

---

#### Задача 2.4: Transaction Processor Worker Pool
**Приоритет**: Критичный  
**Время**: 6 часов  
**Зависимости**: 2.1, 2.2, 2.3, 1.2, 1.3

- [ ] Создать `src/processor/worker.rs`
- [ ] Реализовать worker pool с `tokio::spawn`
- [ ] Интегрировать race RPC запросы
- [ ] Интегрировать парсинг и swap detection
- [ ] Интегрировать кэш
- [ ] Создать канал для swap событий

**Критерии готовности**:
- Параллельная обработка транзакций
- Race запросы работают корректно
- Swap события отправляются в канал
- Общая латентность обработки < 50ms

**Оптимизации**:
- Batch processing для снижения overhead
- Приоритетная очередь для WS сигнатур
- Минимальные аллокации в hot path

---

### Фаза 3: Торговый модуль (Неделя 2)

#### Задача 3.1: Jupiter Client
**Приоритет**: Критичный  
**Время**: 8 часов  
**Зависимости**: 1.2

- [ ] Создать `src/trading/jupiter.rs`
- [ ] Реализовать `get_quote` (v1 и v6)
- [ ] Реализовать `create_swap_tx` (v1 и v6)
- [ ] Реализовать race запросы к Jupiter endpoints
- [ ] Обработка ошибок и retry

**Критерии готовности**:
- Quote запрос < 100ms
- Создание swap транзакции < 50ms
- Поддержка v1 и v6 API
- Race запросы к primary/fallback endpoints

**Оптимизации**:
- Параллельные запросы к нескольким endpoints
- Кэширование quote для повторных запросов
- Минимальные таймауты

---

#### Задача 3.2: Transaction Signer
**Приоритет**: Высокий  
**Время**: 4 часа  
**Зависимости**: Нет

- [ ] Создать `src/trading/signer.rs`
- [ ] Реализовать подпись транзакций с `ed25519-dalek`
- [ ] Конвертация из base64 в транзакцию
- [ ] Подпись и конвертация обратно в base64

**Критерии готовности**:
- Подпись транзакции < 5ms
- Корректная подпись для Solana
- Поддержка VersionedTransaction

---

#### Задача 3.3: Risk Management
**Приоритет**: Высокий  
**Время**: 3 часа  
**Зависимости**: Нет

- [ ] Создать `src/trading/risk.rs`
- [ ] Реализовать cooldown между сделками
- [ ] Реализовать per-token cooldown
- [ ] Реализовать дневной лимит SOL
- [ ] Реализовать минимальные суммы

**Критерии готовности**:
- Все risk проверки работают
- Lock-free реализация для производительности
- Минимальная латентность проверок

---

#### Задача 3.4: Trading Engine
**Приоритет**: Критичный  
**Время**: 10 часов  
**Зависимости**: 3.1, 3.2, 3.3, 1.2

- [ ] Создать `src/trading/engine.rs`
- [ ] Реализовать обработку buy событий
- [ ] Реализовать обработку sell событий
- [ ] Интегрировать Jupiter client
- [ ] Интегрировать signer
- [ ] Интегрировать risk checks
- [ ] Интегрировать shotgun send

**Критерии готовности**:
- Buy операция выполняется < 200ms
- Sell операция выполняется < 200ms
- Все компоненты интегрированы
- Обработка ошибок корректна

**Оптимизации**:
- Параллельная проверка баланса и получение quote
- Минимальные задержки между операциями
- Shotgun отправка на все RPC одновременно

---

### Фаза 4: Интеграция и оптимизация (Неделя 2-3)

#### Задача 4.1: Main Loop
**Приоритет**: Критичный  
**Время**: 6 часов  
**Зависимости**: Все предыдущие задачи

- [ ] Создать `src/main.rs`
- [ ] Инициализировать все компоненты
- [ ] Настроить каналы между компонентами
- [ ] Запустить все async задачи
- [ ] Реализовать graceful shutdown

**Критерии готовности**:
- Все компоненты запускаются корректно
- Каналы настроены правильно
- Graceful shutdown работает
- Нет утечек ресурсов

---

#### Задача 4.2: Analytics и мониторинг
**Приоритет**: Средний  
**Время**: 4 часа  
**Зависимости**: Все предыдущие задачи

- [ ] Создать `src/analytics/stats.rs`
- [ ] Реализовать сбор статистики
- [ ] Реализовать метрики производительности
- [ ] Добавить логирование с `tracing`

**Критерии готовности**:
- Статистика собирается корректно
- Метрики показывают производительность
- Логирование структурированное

---

#### Задача 4.3: Тестирование и оптимизация
**Приоритет**: Высокий  
**Время**: 12 часов  
**Зависимости**: Все предыдущие задачи

- [ ] Написать интеграционные тесты
- [ ] Написать бенчмарки
- [ ] Профилирование с `cargo flamegraph`
- [ ] Оптимизация hot paths
- [ ] Устранение узких мест

**Критерии готовности**:
- Все тесты проходят
- Бенчмарки показывают целевые метрики
- Профилирование не показывает узких мест
- Производительность соответствует требованиям

---

## ⚡ Критичные оптимизации для максимальной скорости

### 1. Zero-copy парсинг
- Использовать `serde_json::raw_value` для больших JSON полей
- Избегать клонирования строк где возможно
- Использовать `Cow<str>` для условного владения

### 2. Lock-free структуры данных
- `DashMap` вместо `Mutex<HashMap>`
- `ArcSwap` для атомарного обновления конфигурации
- `parking_lot::Mutex` вместо `std::sync::Mutex` (быстрее)

### 3. Минимальные аллокации
- Переиспользование буферов
- Pre-allocate векторы с известным размером
- Использовать `SmallVec` для маленьких массивов

### 4. Параллелизм
- Все независимые операции выполняются параллельно
- `tokio::join!` для параллельных await
- `futures::future::select_all` для race операций

### 5. Оптимизация сети
- HTTP/2 для мультиплексирования
- Connection pooling с переиспользованием
- Минимальные таймауты (300-500ms)
- TCP keepalive для долгих соединений

### 6. Оптимизация компиляции
- `opt-level = 3` для максимальной оптимизации
- `lto = true` для link-time optimization
- `codegen-units = 1` для лучшей оптимизации
- Inline атрибуты для hot paths

---

## 🔄 Асинхронная архитектура потоков

### Основной event loop:

```rust
#[tokio::main]
async fn main() -> Result<()> {
    // 1. Инициализация (синхронно)
    let config = Config::load()?;
    let keypair = load_keypair(&config)?;
    
    // 2. Создание компонентов
    let race_client = Arc::new(RaceClient::new(config.rpcs.clone()));
    let jupiter_client = Arc::new(JupiterClient::new());
    let cache = Arc::new(TransactionCache::new(2000));
    
    // 3. Каналы для событий
    let (ws_tx, ws_rx) = mpsc::unbounded_channel(); // WebSocket → Processor
    let (swap_tx, swap_rx) = mpsc::unbounded_channel(); // Processor → Trading
    
    // 4. Запуск WebSocket (async, независимо)
    let ws_manager = WebSocketManager::new(config.ws_url, ws_tx);
    tokio::spawn(async move {
        ws_manager.run(&config.wallet_address).await.unwrap();
    });
    
    // 5. Запуск Processor Pool (async, параллельно)
    let processor = Arc::new(TransactionProcessor::new(
        race_client.clone(),
        cache.clone(),
        swap_tx,
    ));
    
    for _ in 0..config.max_workers {
        let processor_clone = processor.clone();
        let mut transport_rx_clone = transport_rx.clone();
        
        tokio::spawn(async move {
            while let Some(signature) = transport_rx_clone.recv().await {
                processor_clone.process(signature).await.unwrap();
            }
        });
    }
    
    // 6. Запуск Trading Engine (async, параллельно)
    let trading = Arc::new(TradingEngine::new(
        keypair,
        race_client.clone(),
        jupiter_client,
    ));
    
    tokio::spawn(async move {
        while let Some(swap_event) = swap_rx.recv().await {
            let trading_clone = trading.clone();
            
            tokio::spawn(async move {
                trading_clone.execute(swap_event).await.unwrap();
            });
        }
    });
    
    // 7. Ожидание завершения
    tokio::signal::ctrl_c().await?;
    Ok(())
}
```

### Поток обработки транзакции:

#### WebSocket путь:
```
1. WebSocket получает JSON сообщение (0ms)
   ↓
2. Парсинг JSON и извлечение signature (0.5ms) - zero-copy где возможно
   ↓
3. Отправка в канал transport_tx (0.1ms) - неблокирующая
   ↓
4. Worker получает signature (0.5ms)
```

#### gRPC путь (быстрее):
```
1. gRPC получает бинарное сообщение (0ms)
   ↓
2. Десериализация protobuf и извлечение signature (0.2ms) - быстрее JSON
   ↓
3. Отправка в канал transport_tx (0.1ms) - неблокирующая
   ↓
4. Worker получает signature (0.3ms) - быстрее за счет меньшей латентности
```
   ↓
5. Проверка кэша (0.2ms) - lock-free
   ↓
6. Race RPC запрос (параллельно на все RPC) (20-50ms)
   ↓
7. Парсинг транзакции (2-3ms)
   ↓
8. Swap detection (0.5ms)
   ↓
9. Кэширование (0.2ms) - lock-free
   ↓
10. Отправка swap события (0.1ms) - неблокирующая
    ↓
11. Trading Engine получает событие (0.5ms)
    ↓
12. Risk проверки (0.5ms) - lock-free
    ↓
13. Jupiter quote (параллельно с проверкой баланса) (50-100ms)
    ↓
14. Создание swap транзакции (20-30ms)
    ↓
15. Подпись транзакции (3-5ms)
    ↓
16. Shotgun send (параллельно на все RPC) (30-50ms)
    ↓
Итого WebSocket: ~130-250ms от получения до отправки транзакции
Итого gRPC: ~100-200ms от получения до отправки транзакции (на 20-30% быстрее)
```

### Преимущества gRPC:

1. **Бинарный протокол**: Protobuf быстрее JSON парсинга (2-3x)
2. **Меньше overhead**: Меньше данных передается по сети
3. **Streaming**: Нативная поддержка streaming подписок
4. **HTTP/2**: Мультиплексирование запросов
5. **Типобезопасность**: Protobuf гарантирует корректность данных

### Когда использовать gRPC:

- ✅ **Рекомендуется**: Когда доступен gRPC endpoint (Helius, QuickNode и др.)
- ✅ **Максимальная скорость**: Для минимальной латентности
- ✅ **Высокая нагрузка**: При большом количестве транзакций

### Когда использовать WebSocket:

- ✅ **Fallback**: Когда gRPC недоступен
- ✅ **Универсальность**: Работает со всеми Solana RPC провайдерами
- ✅ **Простота**: Не требует специальных gRPC endpoints

---

## 📋 Чеклист готовности

### Функциональность:
- [ ] WebSocket подключение и подписка работают
- [ ] gRPC подключение и streaming подписки работают
- [ ] Переключение между WebSocket и gRPC работает
- [ ] Автоматический выбор транспорта (auto mode) работает
- [ ] Транзакции парсятся корректно
- [ ] Swap обнаружение работает для всех типов
- [ ] Jupiter интеграция работает (v1 и v6)
- [ ] Транзакции подписываются и отправляются
- [ ] Risk management работает
- [ ] Graceful shutdown работает

### Производительность:
- [ ] WebSocket → обработка < 5ms
- [ ] gRPC → обработка < 2ms (быстрее за счет бинарного протокола)
- [ ] Парсинг транзакции < 3ms
- [ ] Race запрос < 50ms
- [ ] Общая латентность (WebSocket) < 200ms
- [ ] Общая латентность (gRPC) < 150ms (на 25% быстрее)
- [ ] Память < 50MB
- [ ] CPU < 10% при нормальной нагрузке

### Надежность:
- [ ] Автоматическое переподключение WebSocket
- [ ] Автоматическое переподключение gRPC
- [ ] Graceful fallback с gRPC на WebSocket при ошибках
- [ ] Retry для RPC запросов
- [ ] Обработка всех типов ошибок
- [ ] Нет утечек памяти
- [ ] Нет паник в release режиме

---

## 🧪 Тестирование

### Unit тесты:
- Парсинг транзакций
- Swap detection
- Risk management
- Подпись транзакций

### Интеграционные тесты:
- WebSocket подключение
- gRPC подключение и streaming
- Переключение между транспортами
- Полный цикл обработки транзакции
- Торговые операции (mock Jupiter)

### Бенчмарки:
- HTTP race запросы
- Парсинг транзакций
- Swap detection
- Полный цикл (WebSocket → Trade)
- Полный цикл (gRPC → Trade) - сравнение производительности
- Сравнение WebSocket vs gRPC латентности

---

## 📈 Метрики успеха

### Производительность:
- ✅ Общая латентность WebSocket < 200ms (цель: < 150ms)
- ✅ Общая латентность gRPC < 150ms (цель: < 120ms) - на 25-30% быстрее
- ✅ Парсинг < 3ms (цель: < 2ms)
- ✅ Race запрос < 50ms (цель: < 30ms)
- ✅ gRPC обработка < 2ms (цель: < 1.5ms)

### Надежность:
- ✅ Uptime > 99.9%
- ✅ Автоматическое восстановление < 5 секунд
- ✅ Нет потери транзакций

### Точность:
- ✅ 100% корректное обнаружение swap
- ✅ 100% корректное выполнение торговых операций
- ✅ 0% ложных срабатываний

---

## 🚨 Риски и митигация

### Риск 1: Высокая латентность сети
**Митигация**: 
- Использовать несколько RPC endpoints
- Race запросы к ближайшим endpoints
- Минимальные таймауты
- **gRPC для снижения латентности на 20-30%**

### Риск 2: Jupiter API задержки
**Митигация**:
- Race запросы к primary/fallback
- Кэширование quote
- Параллельные запросы

### Риск 3: WebSocket/gRPC обрывы
**Митигация**:
- Автоматическое переподключение для обоих транспортов
- Fan-out на несколько WS endpoints
- Graceful fallback с gRPC на WebSocket
- Fallback на polling при полном отказе транспортов
- Health checks для мониторинга состояния соединений

### Риск 4: gRPC недоступность
**Митигация**:
- Auto mode автоматически переключается на WebSocket
- Поддержка fallback на WebSocket при ошибках gRPC
- Проверка доступности gRPC при старте
- Логирование причин переключения транспорта

### Риск 5: Ошибки компиляции Rust
**Митигация**:
- Постепенная разработка
- Тестирование каждого модуля
- Использование стабильных версий библиотек

---

## 📝 Примечания

1. **Приоритет скорости**: Все решения принимаются с приоритетом на минимальную латентность
2. **gRPC предпочтительнее**: Использовать gRPC когда доступен (быстрее на 20-30%)
3. **Lock-free везде где возможно**: Использовать `DashMap`, `ArcSwap` вместо мьютексов
4. **Параллелизм**: Все независимые операции выполняются параллельно
5. **Минимальные аллокации**: Переиспользование буферов, pre-allocation
6. **Профилирование**: Постоянное профилирование и оптимизация hot paths
7. **Transport abstraction**: Единый интерфейс для WebSocket и gRPC позволяет легко переключаться

---

## 🎯 Итоговые цели

Создать бот на Rust, который:
- ⚡ Работает **4-5x быстрее** чем Python версия
- 🎯 Обнаруживает и выполняет сделки за **< 200ms** (WebSocket) или **< 150ms** (gRPC)
- 🚀 **gRPC режим на 20-30% быстрее** WebSocket за счет бинарного протокола
- 🔄 **Гибкое переключение** между WebSocket и gRPC через конфигурацию
- 💾 Потребляет **3-5x меньше** памяти
- 🔒 Имеет **99.9% uptime** с автоматическим восстановлением
- 🚀 Готов к production использованию

---

**Версия**: 1.0  
**Дата создания**: 2024  
**Автор**: AI Assistant  
**Статус**: В разработке

